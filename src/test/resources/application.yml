spring:
  application:
    name: task-ms
    group:
      test:
        - db-test
  config:
    import:
      - application-logging.yml
      - application-db.yml
      - application-actuator.yml
      - application-security-local.yml
logging:
  level:
    sql: debug
